var multer = require('./multer'),
    authenticate = require('./authenticate');
authbrowser = require('./authbrowser');

// ========================== Export Module Start ==========================
module.exports = {
    multer,
    authenticate,
    authbrowser
}
// ========================== Export Module End ============================